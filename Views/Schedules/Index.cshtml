@model IEnumerable<JournalKIT.Schedule>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Datelessons)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Groupstudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lessons)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timelessons)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tutor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Datelessons)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Groupstudent.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lessons.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Timelessons.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tutor.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
