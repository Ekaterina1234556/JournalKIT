@model IEnumerable<JournalKIT.Models.Journalofduty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dateduty)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Groupstudent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dateduty)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Groupstudent.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student1.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student2.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
