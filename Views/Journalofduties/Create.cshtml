@model JournalKIT.Models.Journalofduty

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Journalofduty</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Dateduty" class="control-label"></label>
                <input type="date" asp-for="Dateduty" class="form-control" />
                <span asp-validation-for="Dateduty" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Groupstudentid" class="control-label"></label>
                <select asp-for="Groupstudentid" class ="form-control" asp-items="ViewBag.Groupstudentid"></select>
            </div>
            <div class="form-group">
                <label asp-for="Student1id" class="control-label"></label>
                <select asp-for="Student1id" class ="form-control" asp-items="ViewBag.Student1id"></select>
            </div>
            <div class="form-group">
                <label asp-for="Student2id" class="control-label"></label>
                <select asp-for="Student2id" class ="form-control" asp-items="ViewBag.Student2id"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
